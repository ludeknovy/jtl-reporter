version: '2.1'

services:
  fe:
    image: novyl/jtl-reporter-fe:v4
    ports:
     - "2020:80"
    depends_on:
      - db
      - be

  db:
    image: novyl/jtl-reporter-db
    build:
      context: ./db/
      dockerfile: Dockerfile
    volumes:
      - ./data/jtl_reporter:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 10
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust

  be:
    image: novyl/jtl-reporter-be:v4
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=db
      - JWT_TOKEN=27JU4qy73hchTMLoH8w9m # please change this token
      - JWT_TOKEN_LOGIN=GdK6TrCvX7rJRZJVg4ijt  # please change this token, the same must be used for listener service

  migration:
    image: novyl/jtl-reporter-be:v4
    environment:
        - DATABASE_URL=postgres://postgres@db/jtl_report
    command: npm run migrate up
    depends_on:
      db:
        condition: service_healthy
  
  listener:
    image: novyl/jtl-reporter-listener-service:v2
    ports:
      - "6000:6000"
    environment: 
      - DB_HOST=db
      - JWT_TOKEN=GdK6TrCvX7rJRZJVg4ijt # paste the same token as in be service - JWT_TOKEN_LOGIN
    
    

